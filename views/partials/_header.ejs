<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
  <body>
    <header><b>SHORTENER URL APP</b></header>
    <% if (user) { %>
      <%= user.email %>
      <form action='/logout' method='POST'>
        <button>Logout</button>
      </form>
    <% } else { %>  
      <a href="login">Log in</a>
      <a href="register">Sign up</a>
    <% } %>
    
  </body>
</html>




